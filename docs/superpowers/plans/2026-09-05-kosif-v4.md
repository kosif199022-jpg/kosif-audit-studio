# KOSIF 4 Implementation Plan

> **For agentic workers:** Use superpowers:executing-plans for inline integration and read-only subagent review where helpful. The site owner makes source edits.

**Goal:** Upgrade the supplied audit studio into a responsive agent and review workspace.
**Architecture:** Preserve the static ESM app and existing local state. Add pure agent/reference modules and one UI adapter; financial calculations stay in the existing engine.
**Tech Stack:** Browser ES modules, CSS, Node test runner, existing Web Speech API.
**Spec:** `docs/superpowers/specs/2026-09-05-kosif-v4-design.md`

## Global Constraints

- Preserve every existing audit screen, deterministic BigInt engine, local engagement, exports, voice assistant and human approval boundaries.
- Explicitly identify the local rules mode; never imply a model call that did not occur.
- Completing a task never closes a risk, approves a report or changes evidence.
- Applicability uses a chosen reporting-period start, not today's date.
- Browser QA is not requested and is not claimed.

### Task 1: Import and verify supplied baseline
- [x] Import v3.2 into `codex/kosif-v4-agent-studio`, preserving main history.
- [x] Run `node --test tests/*.test.js`; distinguish inherited failures from changes.

### Task 2: Agent, references and reviewer contracts
Files: `agent.js`, `reference-registry.js`, `tests/agent.test.js`, `tests/references.test.js`, `council.js`, `analytics.js`.
- [x] Add tests for empty inputs, deterministic task ordering, safe task transitions, state fingerprints and reporting-period boundaries.
- [x] Implement plan and memo generation using current evidence, risks and official reference links.
- [x] Normalize council gates; strengthen missing-input stances and conflict decisions.
- [x] Fix opinion draft without a materiality assessment; verify regressions.

### Task 3: Cinematic responsive experience
Files: `studio.js`, `studio.css`, `index.html`, `app.js`, `voice.js`.
- [x] Integrate state, context and actions; add agent, workflow and reference screens.
- [x] Add source freshness banners, reviewer references, due-date/assignee editor and memo export.
- [x] Add editorial dashboard, cinematic CSS, dark mode, reduced motion and bottom navigation.
- [x] Retain accessible labels, focus, existing dialogs and all prior screens.

### Task 4: Release verification and delivery
Files: `sw.js`, `package.json`, `README.md`, `CHANGELOG.md`, `scripts/check-static.mjs`.
- [x] Limit PWA caching to public shell assets and bump cache version.
- [x] Run full tests, syntax and static asset checks; review final changes.
- [ ] Commit, push feature branch and open reviewable PR; package full source for delivery.

## Decisions

- The user authorized feature/design discretion. Continue implementation without a new design approval round.
- The current Cloudflare Worker has service bindings unlike this static repository; do not overwrite it with a static build.
- Use local grounded assistance and explicit data provenance. No model subscription, API-key collection or background transmission is added.

## Verification record

2026-09-05: Node suite passed 56/56 tests. Static checks passed: 18 views, 345 static element IDs, 14 local asset/import references, all top-level JavaScript syntax, CSS delimiters and manifest shortcuts. `git diff --check` passed. Independent source review identified and resolved reference-intent matching and stale council/memo provenance. Browser QA and production deployment were not requested and have not been performed.
