{
  "name": "kosif-audit-studio",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "description": "KOSIF Audit Studio — deterministic Arabic-first audit workspace",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-static.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
