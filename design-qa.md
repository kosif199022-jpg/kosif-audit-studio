# Design QA — KOSIF Audit Studio

## النتيجة

**PASS مع حدود موثقة** — ربط الميزان بالمعيار، لوحة الأوامر، مساحة المراجع، مركز النتائج، والتقارير المهنية تعمل في معاينة Sites المحلية على نافذة سطح المكتب الثابتة. لم يُستخدم تصوير قديم كدليل على النسخة المنفذة.

## الدليل المرئي الحالي

- مرجع KOSIF Stable الملتقط في نافذة سطح المكتب: `/workspace/scratch/kosif-stable-audit/03-trial-balance.jpg` (`1353 × 929`).
- ربط IAS 21 السياقي بعد إصلاح التباين: `/workspace/scratch/kosif-v7-qa/standards-ias21-fixed.jpg`.
- المقارنة المجمعة في الحالة نفسها: `/workspace/scratch/910c3a2b27ca/tmp/design-qa/stable-vs-v7-standards.jpg`.
- مراجع الهاتف المرفقة: `IMG_5547.png` و`IMG_5548.png` و`IMG_5549.jpeg` داخل مجلد الرفع الحالي.

حالة المقارنة واحدة: مركز المعايير في المظهر الداكن وبنفس نافذة سطح المكتب. تحافظ النسخة المنفذة على المكتبة والبحث والأدوات المهنية المرصودة، وتضيف بطاقة سياق حسابي توضح الرمز والعملة الوظيفية وعملة الحساب وسبب تطبيق IAS 21. كشف الفحص الأول ضعف تباين بطاقات المعايير في المظهر الداكن؛ استُبدلت الخلفيات الثابتة بألوان نظام المظهر، ثم أُعيد الالتقاط وتأكد التباين بصريًا وحسابيًا. لا يُدّعى تطابق pixel-perfect مع لقطات الهاتف؛ الاستجابة المحمولة مغطاة بعقود CSS والاختبارات الآلية، وتحتاج نافذة هاتف فعلية وقارئ شاشة قبل أي ادعاء WCAG شامل.

## فحص الرحلة

1. فتح لوحة الأوامر بـ`Ctrl+K`: ظهر `dialog` مسمى، وعثر البحث على IAS 21 والحسابات والجولات والأدلة، ونجح الانتقال إلى المعيار.
2. البحث في الميزان عن `حساب USD`: ظهر حسابان بالدولار مع العملة الوظيفية SAR وسعر الإقفال، وفتح زر IAS 21 بطاقة المعيار من الحساب `110041`.
3. بطاقة IAS 21: عرضت سبب الربط الخاص بالحساب، الإجراءات والدليل المتوقع، التعرض لأربعة حسابات فقط، موضع المرجع، والمصادر الرسمية.
4. مساحة المراجع: عرضت 20 طلب PBC معتمدًا، وثلاثة تبويبات وظيفية، ومحدد الحساب بحد أقصى 100 نتيجة إضافة إلى خيار فارغ، مع بحث مستقل.
5. المخرجات: ظهرت مسودة خطاب الإدارة، مصفوفة الالتزام الحية، المسائل غير المحسومة، Word والطباعة، وروابط فتح كل معيار.
6. فحص العرض: لا يوجد تمرير أفقي للصفحة عند عرض الميزان أو المعايير أو مساحة المراجع أو التقارير؛ الجداول الداخلية العريضة تستخدم حاوية تمرير مخصصة.

## نقاط القوة والمخاطر

- مركز المعايير يحافظ على كثافة المرجع في المظهر الداكن مع تباين مقروء وربط سياقي أوضح.
- مركز النتائج يجيب مباشرة عن سؤال «كل نتائج التطبيق» ويعرض مصدر كل مجموعة وعملتها وحدودها.
- التنقل الدلالي، العناوين، `progressbar`، الأزرار، و`dialog` ظاهرة في DOM؛ الواجهة تدعم التخطي إلى المحتوى وإغلاق النافذة بـEscape.
- يلزم اختبار فعلي بقارئ شاشة وVoiceOver وبنافذة هاتف حقيقية قبل أي ادعاء امتثال WCAG كامل.
- التخزين محلي في `localStorage` وIndexedDB وليس سجل WORM أو RBAC؛ الميزان المستورد خاص بالجلسة ويجب تنزيل لقطته قبل إعادة التحميل.
- PDF وXLSX ثابتان لسيناريو العرض v7، بينما JSON وCSV يعكسان الجلسة الحالية.

## التحقق الآلي والمخرجات

- `npm test`: ‏116/116 ناجحة، وتشمل البناء الإنتاجي.
- `npm run test:sites`: ‏6/6 ناجحة.
- PDF: ‏14 صفحة A4، وفحص بصري لكل الصفحات بعد إصلاح تداخل صفحة المعايير وملاحظة الاستمرارية.
- XLSX: ‏12 ورقة، 5,000 حساب، 58 معيارًا، 4 حسابات USD/EUR مرتبطة بـIAS 21، و11/11 فحص مصالحة `OK`، بلا أخطاء صيغ مكتشفة.
