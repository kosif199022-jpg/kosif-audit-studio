# تشغيل KOSIF Audit Studio في Google AI Studio

هذه نسخة React/Vite مستقلة يمكن رفعها إلى مساحة تطبيقات Google AI Studio أو تشغيلها محليًا. التطبيق الحالي حتمي ويعمل بالكامل في المتصفح؛ لذلك **لا يحتاج مفتاح Gemini API** ما لم تضف لاحقًا قدرات ذكاء اصطناعي تتصل بخدمة خارجية.

## الرفع إلى AI Studio

1. فك ضغط حزمة `KOSIF_AUDIT_STUDIO_AI_STUDIO.zip`.
2. افتح [Google AI Studio](https://aistudio.google.com/) وسجّل الدخول.
3. من مساحة بناء التطبيقات، اختر استيراد/رفع مشروع موجود (قد يختلف اسم الزر حسب إصدار الواجهة)، ثم ارفع مجلد المشروع أو اربطه بمستودع GitHub.
4. إذا طلبت البيئة أمر التثبيت استخدم `npm install`، وللتشغيل استخدم `npm run dev`.
5. للبناء المستقل استخدم `npm run build:ai`؛ ستجد الناتج داخل `dist-ai/`.

إذا كانت واجهة AI Studio لا تعرض خيار ZIP، فك الضغط أولًا وارفع الملفات أو استخدم تكامل GitHub. لا تضع مفتاح API داخل ملفات المصدر أو `localStorage`؛ استخدم إدارة الأسرار في منصة الاستضافة عندما تضيف اتصالًا بخدمة خارجية.

## الأوامر

```bash
npm install
npm run dev
npm run build:ai
```

## حدود النسخة الحالية

- البيانات المرفقة داخل التطبيق **تجريبية ومولدة حتميًا**: ملف ارتباط مكتمل من 5,000 حساب موزعة على 20 مجالًا محاسبيًا، مع نتائج الربط والتحليلات والجولات والأدلة والتسويات والمجلس والتقرير.
- تحفظ بيانات حالة الارتباط الوصفية في `localStorage`، وتحفظ بايتات الأدلة المرفوعة في IndexedDB داخل المتصفح؛ لا توجد قاعدة بيانات خادمية أو مزامنة متعددة المستخدمين. الميزان المستورد مؤقت للجلسة ويمكن تنزيل لقطته قبل إعادة التحميل.
- استيراد البيانات يدعم XLSX/XLS (الورقة الأولى) وCSV/TSV/TXT حتى 15 MB بعد التحضير والفحص؛ لا يدعم OCR أو استخراج ميزان من PDF/DOCX.
- مجلس المراجعين الحالي محرك تحليلي حتمي بلا اتصال بمزود AI، ولا يختار الرأي أو يعتمد التقرير.
- البصمات وقفل الفترات وسجل القرارات ضوابط محلية قابلة للتحقق، وليست سجل WORM أو RBAC مؤسسيًا.
- ربط المعايير عبارة عن ملخصات تشغيلية وبيانات وصفية وإجراءات مقترحة، وليس نسخًا كاملة من النصوص الرسمية ولا بديلًا عن حكم المراجع المرخّص.
- لا توجد أسرار أو مفاتيح API مطلوبة لتجربة القدرات الحالية.
